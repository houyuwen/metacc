# tools/metacc/CMakeLists.txt
#
# metacc.h must stay in this directory for source runs, and in the standalone
# release folder for packaged runs.  Do not copy/install it into build-time
# working directories here; projects should include this directory directly.
